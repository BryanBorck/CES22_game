scale_factor

monster_images def
bear_images def

process_enemies

girl dead def + character

set max_hearts (min)

import time

die function

move and process blocks (enemy)

surface.blit(score_text, (WIDTH - score_text.get_width() - 32, 32))